import 'package:flutter/material.dart';

abstract class AppIconWidgets {
  static const humidityIcon = Icon(
    Icons.water_drop_rounded,
    size: 20,
    color: Colors.white,
  );

  static const humiditySmallIcon = Icon(
    Icons.water_drop_rounded,
    size: 15,
    color: Colors.white,
  );

  static const windSpeedIcon = Icon(
    Icons.waves_rounded,
    size: 20,
    color: Colors.white,
  );
}
